<UserControl x:Class="OctoConverter.Controls.FileListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             AllowDrop="True" DragOver="OnDragOver" Drop="OnDrop">
    <DockPanel>
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <Button Content="＋ 파일 추가" Click="AddFiles_Click" Margin="0,0,8,0"/>
                <Button Content="선택 제거" Click="RemoveSelected_Click" Margin="0,0,8,0"/>
                <Button Content="모두 지우기" Click="Clear_Click"/>
            </StackPanel>
            <TextBlock x:Name="CountText" HorizontalAlignment="Right" VerticalAlignment="Center"
                       Foreground="{StaticResource TextSecondaryBrush}"/>
        </DockPanel>

        <Border Style="{StaticResource Card}" Padding="4">
            <Grid>
                <ListView x:Name="List" BorderThickness="0" Background="Transparent"
                          SelectionMode="Extended" AllowDrop="True"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="파일" Width="250" DisplayMemberBinding="{Binding FileName}"/>
                            <GridViewColumn Header="크기" Width="80" DisplayMemberBinding="{Binding SizeText}"/>
                            <GridViewColumn Header="정보" Width="170" DisplayMemberBinding="{Binding Info}"/>
                            <GridViewColumn Header="상태" Width="70" DisplayMemberBinding="{Binding Status}"/>
                            <GridViewColumn Header="진행" Width="110">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <ProgressBar Value="{Binding Progress, Mode=OneWay}" Maximum="100"
                                                     Height="8" Width="90"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="결과" Width="170" DisplayMemberBinding="{Binding ResultText}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
                <TextBlock x:Name="EmptyHint" Text="여기로 파일이나 폴더를 끌어다 놓으세요"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Foreground="{StaticResource TextSecondaryBrush}" FontSize="14"
                           IsHitTestVisible="False"/>
            </Grid>
        </Border>
    </DockPanel>
</UserControl>
